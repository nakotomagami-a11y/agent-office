{
  "name": "agent-office",
  "version": "0.0.7-beta",
  "private": true,
  "license": "GPL-3.0-or-later",
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "dev": "pnpm --filter @agent-office/web dev",
    "build": "pnpm --filter @agent-office/web build",
    "start": "pnpm --filter @agent-office/web start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "install:local": "bash scripts/install-local.sh"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
